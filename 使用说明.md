# 应县木塔 · 一木千年

六幕连续滚动的三维科普网站，附可编辑木塔模型、光影场景与渲染图片。建筑由真实三维网格显示，近景使用塔上原构件。

## 打开网站

Mac 双击「启动网站.command」，自动选择空闲端口并打开浏览器。保持终端窗口开启。也可执行 `python3 启动网站.py`，需要本机有 Python 3。

本次预览：[打开网站](http://127.0.0.1:5188/)。仅在本机服务运行期间有效。压缩包解压后可以独立启动，无需联网加载模型、字体或图片。

## 六幕顺序

1. 初见：整塔立于天地之间。
2. 解构：斜向展开，只截取中间三层。
3. 柱网：锁定第三层，屋面移出，观察内外柱网。
4. 斗栱：推进同一层的柱头，读懂承托。
5. 飞檐：构件归位，沿檐拉远。
6. 守护：回到完整全景。

向上滚动会反向归位。顶部导航可直达对应篇章。

「自由观塔」支持拖动旋转、缩放、拆层、隐藏屋面、剖看内部、选择楼层及全貌／正面／俯瞰／檐下视角。点击「恢复初始视角」恢复全部构件；Esc 返回原来的阅读位置。

模型获得键盘焦点后，左右方向键旋转，＋／－缩放。页脚可开启「减少动态效果」，也尊重系统设置。手机采用独立取景与文字排布。

## 文件

| 文件 | 用途 |
|---|---|
| `yingxian-pagoda/dist/` | 可直接部署到静态网站空间的成品 |
| `yingxian-pagoda/src/` | 网站源代码 |
| `model/应县木塔.blend` | 可编辑建筑模型 |
| `model/应县木塔_六幕光影.blend` | 带相机、灯光、天空和庭院的场景 |
| `model/应县木塔_通用.glb` | 可导入其他三维软件的通用模型 |
| `model/重建模型.py` | 基础建模脚本 |
| `model/精修木构材质.py` | 扫描木纹、边缘和近景凹槽 |
| `model/修正平座连接.py` | 平座承托、屋面收口与脊饰修正 |
| `model/修复斗栱完整性.py` | 完整斗栱与逐件核验的浅凹槽 |
| `model/六幕光影渲染.py` | 六幕场景生成脚本 |
| `model/渲染六幕.py` | 从现成光影场景输出六张图片 |
| `设计对照/逐幕对照.html` | 设计与新版页面并排展示，可切换修改前 |
| `model/对齐设计与渲染.py` | 本轮字体背景对照后更新的光影与相机脚本 |
| `结构修正/连接处对照.html` | 栏杆下方修改前后对照 |
| `光影材质升级/修改对照.html` | 六幕背景、木纹及光影对照 |
| `渲染验证/` | 实际网页截图、Blender 渲染及浏览页 |
| `第三版设计/` | 用户认可的六幕设计参考，区别于实际渲染 |

光影场景的六个定稿镜头位于第 1、101、201、301、401、501 帧，对应时间线标记。它们用于静帧渲染；网站的连续镜头、构件渐隐和文字动画由网页控制。

## 修改与重建

网站：进入 `yingxian-pagoda` 后执行 `npm install`、`npm run dev`；发布构建执行 `npm run build`。网站需要通过本地服务或网站空间打开，不能双击 HTML 文件运行三维部分。

模型：用 Blender 打开 `.blend`。从全新 Blender 文件重建时，依次执行 `重建模型.py`、`六幕光影渲染.py`、`精修木构材质.py`、`修正平座连接.py`、`修复斗栱完整性.py`。现成交付模型已包含这些修正，无需重复运行。脚本会更新同名输出，修改前请备份。重新导出后，在网站目录执行 `node scripts/compress-model.mjs`，再执行 `npm run build`。

六幕渲染：用 Blender 打开光影场景后执行 `渲染六幕.py`。此脚本调用「对齐设计与渲染.py」，应用最新背景、相机与光影后出图；`六幕光影渲染.py` 会从建筑场景重新布置六幕，并更新同名文件。

## 复原范围

依据公开尺寸、照片和结构描述制作，属于建筑科普重建。总高按 67.31 米建立，包含八角塔身、五层六檐、暗层、双圈柱网、梁枋斜撑、代表性斗栱、回廊栏杆、格扇、楼梯、瓦垄、脊饰、风铎、铁刹、匾额和石台。

具体构件尺寸、装饰位置和部分内部构造为展示性推演；未逐一复刻实塔全部 54 种斗栱，未复原佛像、壁画和完整寺院。天空、树影和庭院用于展示氛围。这不是文物测绘或修缮模型。

## 最新细节修正

重做第二至第五层平座下方的承梁、坐斗、挑梁与收口；调整对应屋面内缘及屋脊饰件位置。木材采用真实扫描木纹，近景补充浅凹槽；背景增加连续变化的墨绿金色雾光，侧光和檐下补光分别调整。滚动隐去构件时使用连续收起，消除原先随机颗粒效果。

木材素材：[Poly Haven Rough Wood](https://polyhaven.com/a/rough_wood)，[CC0 许可](https://polyhaven.com/license)。

## 前次视觉对照更新

标题采用真实粗宋体并重新校准字号、行距与位置；首尾两幕分别使用根据设计生成的云雾背景。调整木构侧光、补光和阴影，收暗石台及庭院，并放大拆层、柱网和斗栱镜头。桌面与手机六幕已重新截图检查。

双击「设计对照/逐幕对照.html」可以并排查看设计和实际页面，并切换修改前。设计图中的建筑细部、纹理和景深仍与实时模型有差异。

## 最新镜头与质感试验

网站已加入接缝阴影、柔和反射和近景虚实；首页与斗栱镜头重新校准。打开网站的 `quality.html` 可比较设计稿、修改前、本轮实时画面和实际预渲染样片。

「质感与镜头对照」保存本轮样片和说明。当前有两张定帧样片，完整预渲染滚动序列尚未制作。原六幕光影和渲染验证保留上一轮版本，供对照使用；最新网页镜头记录在 `asset-index.json` 和网页源码中。


本轮首屏再次对齐：64° 广角仰视、暖色侧光、独立前景松树与滚动视差；细节见「质感与镜头对照/首屏再对齐.md」。预渲染定帧仍为上轮版本。


## 第十版光影与排版

网站新增 `alignment.html`，逐幕并排查看原稿与本轮网页，可切换修改前。调整暖色侧光、环境补光、瓦面、背景渐变，以及标题和正文的排布。斗栱镜头单独采用较低的侧光。建筑形体和模型细部仍与设计稿有差异。`quality.html` 保留第九版试验，完整预渲染滚动序列仍未制作。


## 第十一版模型与环境

最新可编辑模型为 `model/应县木塔_环境精修.blend`，通用文件为同名 `.glb`。新增石栏、砌石、1551块独立成形的木地板和配殿；网页院落重新布置铺地、树干与透明树冠。重新导出用 `node scripts/compress-refined.mjs`。模型共保留七个楼层分组，网页加载的新模型为约21.2 MB。

柱网采用较低俯视；斗栱从柱头另一侧取景，围护在此幕恢复，前景匾额暂时隐去以露出结构。飞檐放大并左移，全景适当放大且保留台基。按六幕校正背景亮度和木色，并柔化阴影。最新对照仍在 `alignment.html`，修改前对应第十版。原有离线渲染和压缩包属于此前版本。
